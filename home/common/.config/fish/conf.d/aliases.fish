# Aliases

alias p 'pnpm'
alias oc 'opencode'
alias gg 'lazygit'

# git aliases

alias ga 'git add'
alias gs 'git status'
alias gc 'git commit'
alias gco 'git checkout'
alias gl 'git log'
alias gp 'git pull'
alias ggp 'git push'
alias ggpf 'git push --force'
alias gac 'git add -A && git commit'